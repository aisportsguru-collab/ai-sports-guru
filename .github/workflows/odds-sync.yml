name: Odds & Games Sync (legacy, disabled on push)
on:
  workflow_dispatch:
    inputs:
      league: { description: "nba|nhl|mlb|nfl|ncaaf", required: true, default: nba }
      days:   { description: "Days ahead", required: true, default: "7" }
  schedule:
    - cron: "*/45 * * * *"
permissions: { contents: read }
concurrency: { group: odds-games-legacy-${{ github.ref }}, cancel-in-progress: true }
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow retained for history; use odds-sync-v2.yml instead."
